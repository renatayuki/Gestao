<%= form_for(@receitum) do |f| %>
  <% if @receitum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@receitum.errors.count, "error") %> prohibited this receitum from being saved:</h2>

      <ul>
      <% @receitum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :medicamento %><br />
    <%= f.text_field :medicamento %>
  </div>
  <div class="field">
    <%= f.label :dosagem %><br />
    <%= f.text_field :dosagem %>
  </div>
  <div class="field">
    <%= f.label :frequencia %><br />
    <%= f.text_field :frequencia %>
  </div>
  <div class="field">
    <%= f.label :duracao %><br />
    <%= f.text_field :duracao %>
  </div>
  <div class="field">
    <%= f.label :observacao %><br />
    <%= f.text_area :observacao %>
  </div>
  
  <div class="field">
    <%= f.label :medico %><br />
    <%= collection_select (:receitum, :medico_id, Medico.all, :id, :nome)  %>
  </div>

  <div class="field">
    <%= f.label :paciente %><br />
    <%= collection_select (:receitum, :prontuario_id, Prontuario.all, :id, :nome)  %>
  </div>

  <div class="field">
    <%= f.label :data %><br />
    <%= f.date_select :data %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
