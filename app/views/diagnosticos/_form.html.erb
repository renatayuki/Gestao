<%= form_for(@diagnostico) do |f| %>
  <% if @diagnostico.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@diagnostico.errors.count, "error") %> prohibited this diagnostico from being saved:</h2>

      <ul>
      <% @diagnostico.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descricao %><br />
    <%= f.text_area :descricao %>
  </div>
  <div class="field">
    <%= f.label :data %><br />
    <%= f.date_select :data %>
  </div>

  <div class="field">
    <%= f.label :medico %><br />
    <%= collection_select (:diagnostico, :medico_id, Medico.all, :id, :nome)  %>
  </div>

  <div class="field">
    <%= f.label :paciente %><br />
    <%= collection_select (:diagnostico, :prontuario_id, Prontuario.all, :id, :nome)  %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
